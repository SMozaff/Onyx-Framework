[package]
name = "profile-domain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "ONYX StaffProfile aggregate: pure domain logic for staff profile identity, org info, and work-stats visibility gating."

[dependencies]
platform-kernel = { path = "../../kernel/platform-kernel" }
platform-contracts = { path = "../../kernel/platform-contracts" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
proptest = "1.0"
